This directory has files that are the same for all digital simulation tasks.

The binary file, byterun.cma, is from a build of
Coq, in its _build/default/kernel/byterun directory.

The float64.* and uint63.* sources are from the
Coq kernel.
